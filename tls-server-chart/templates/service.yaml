apiVersion: v1
kind: Service
metadata:
  name: {{ include "tls-server.fullname" . }}
  labels:
    {{- include "tls-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tls-server.selectorLabels" . | nindent 4 }}
