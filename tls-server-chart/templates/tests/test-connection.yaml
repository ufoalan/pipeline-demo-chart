apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tls-server.fullname" . }}-test-connection"
  labels:
    {{- include "tls-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tls-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
